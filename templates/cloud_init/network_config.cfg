version: 2
ethernets:
  ${interface}:
    dhcp4: false
    dhcp6: false
    addresses:
     - ${ip}
    gateway4: ${gateway}
    nameservers:
      search: ${search_domains}
      addresses: ${dns_servers}